<Project>
  <PropertyGroup>
    <!-- Versión central de .NET para todos los proyectos -->
    <TargetFramework>net10.0</TargetFramework>

    <!-- Opcionales, también centralizados -->
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
</Project>